{
  "name": "Tesla clone",
  "version": "1.0.0",
  "description": "This is a websiteis for tailwind practice",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server",
    "build": "tailwind build -i src/input.css -o dist/output.css",
    "watch": "tailwind build -i src/input.css -o dist/output.css -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0",
    "tailwindcss": "^3.2.7"
  },
  "dependencies": {
    "bootstrap": "^4.0.0",
    "cssnano": "^5.1.15",
    "jquery": "^3.3.1",
    "popper.js": "^1.12.9"
  }
}
